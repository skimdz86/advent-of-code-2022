fn main() {
    println!("Hello, world!");

    // modello lista posizioni passate con matrice
    // per capire quando si muove la tail, basta che gli indici di H siano >=T+2 i <=T-2 in ogni direzione (ne basta 1 >=2)
}
